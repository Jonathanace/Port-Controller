import numpy as np

ship = np.full((8,12),"NA")
buffer = np.full((4,24),"NA")
loadingzone = np.full((1),"NA")


def unload(cargo_list, ship,loadingzone):

    return
def load(cargo_list, ship):
    return